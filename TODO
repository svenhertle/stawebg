* stawebg.json: change url or menu title for content files
* Use more regex: excludes, hidden, ...)
* Blog feature (e.g. for news)
* clean up output directory before or (preferably) after building the sites
